{
  "name": "kyrae",
  "private": true,
  "packageManager": "pnpm@10.16.0",
  "engines": {
    "node": ">=24.15.0 <25",
    "pnpm": ">=10.16.0"
  },
  "scripts": {
    "preinstall": "node -e \"const ua=process.env.npm_config_user_agent||''; if(!ua.includes('pnpm')){console.error('Use pnpm to install dependencies.'); process.exit(1)}\"",
    "frontend:dev": "pnpm --dir apps/frontend start",
    "frontend:build": "pnpm --dir apps/frontend build",
    "frontend:lint": "pnpm --dir apps/frontend lint",
    "frontend:test": "pnpm --dir apps/frontend test",
    "frontend:e2e": "pnpm --dir apps/frontend e2e",
    "backend:dev": "pnpm --dir apps/backend start:dev",
    "backend:build": "pnpm --dir apps/backend build",
    "backend:lint": "pnpm --dir apps/backend lint",
    "backend:test": "pnpm --dir apps/backend test",
    "backend:test:e2e": "pnpm --dir apps/backend test:e2e",
    "stt-gateway:dev": "pnpm --dir apps/stt-gateway start:dev",
    "stt-gateway:build": "pnpm --dir apps/stt-gateway build",
    "stt-gateway:lint": "pnpm --dir apps/stt-gateway lint",
    "docker:db": "docker compose up -d postgres",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "docker:restart": "docker compose down && docker compose up --build",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.0",
    "@commitlint/config-conventional": "^21.2.0",
    "@eslint/js": "^10.0.1",
    "eslint": "^10.6.0",
    "eslint-plugin-prettier": "^5.5.6",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8",
    "prettier": "^3.9.4",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.62.1"
  }
}
